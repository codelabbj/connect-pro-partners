import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  "appId": "com.connectpro.app",
  "appName": "connect-pro-partner",
  "webDir": "out"
}
;

export default config;
